- script: |
        echo "Fetching job IDs..."
        job_ids=$(databricks jobs list --output json | jq -r '.[] | .job_id')
        echo "Pausing jobs..."
        for job_id in $job_ids; do
          echo "Pausing job ID: $job_id"
          databricks jobs update --job-id $job_id --json '{ "schedule": null }'
        done
      displayName: "Pause All Scheduled Jobs"
      env:
        DATABRICKS_HOST: $(DATABRICKS_HOST)
        DATABRICKS_TOKEN: $(DATABRICKS_TOKEN)



-----------------------------------------------------
Ahh got it—that’s a **different requirement** 👍
You don’t want one rule with optional fields… you want **completely different behavior per environment**:

* **DEV → Delete blobs**
* **PROD → Archive blobs (no delete)**

👉 This is the correct way to do it: **separate rules using conditional logic**

---

# ✅ **Clean Terraform Solution (Env-based Rules)**

## **1. Locals**

```hcl id="4p8x1n"
variable "environment" {
  type = string
}

locals {
  is_dev  = var.environment == "dev"
  is_prod = var.environment == "prod"
}
```

---

## **2. Lifecycle Policy with Conditional Rules**

```hcl id="1n6zqz"
resource "azurerm_storage_management_policy" "lifecycle" {
  storage_account_id = azurerm_storage_account.example.id

  # ✅ DEV → DELETE RULE
  dynamic "rule" {
    for_each = local.is_dev ? [1] : []

    content {
      name    = "delete-old-data-dev"
      enabled = true

      filters {
        blob_types   = ["blockBlob"]
        prefix_match = ["archivepoc/"]
      }

      actions {
        base_blob {
          delete_after_days_since_modification_greater_than = 30
        }
      }
    }
  }

  # ✅ PROD → ARCHIVE RULE
  dynamic "rule" {
    for_each = local.is_prod ? [1] : []

    content {
      name    = "archive-old-data-prod"
      enabled = true

      filters {
        blob_types   = ["blockBlob"]
        prefix_match = ["archivepoc/"]
      }

      actions {
        base_blob {
          tier_to_archive_after_days_since_modification_greater_than = 365
        }
      }
    }
  }
}
```

---

# 🧠 **Behavior**

| Environment         | Action                 |
| ------------------- | ---------------------- |
| **dev**             | Delete after 30 days   |
| **prod**            | Archive after 365 days |
| **test (if added)** | No rule unless defined |

---

# ✅ **Why This is the Right Approach**

* ✔ Keeps rules **simple and explicit**
* ✔ Avoids Terraform limitations (no dynamic keys)
* ✔ Matches real-world usage:

  * Dev → cost cleanup
  * Prod → compliance retention

---

# ⚙️ **How to Run**

```bash id="n7xtlq"
terraform apply -var="environment=dev"
terraform apply -var="environment=prod"
```

---

# 🚀 **Optional Enhancement**

If you later want:

* Dev → delete after 15 days
* Test → cool → archive
* Prod → archive only

You can extend with more `dynamic "rule"` blocks.

---

If you want next step, I can:

* Convert this into a **reusable Terraform module**
* Align with your **ADF / Databricks storage layers (raw, curated, etc.)**
* Or add **cost impact comparison (Dev vs Prod savings)**

_________________
Here’s the updated service request with your clarification included:

Short Description
Need permission to access required resource for Terraform deployment pipeline in Azure DevOps (TF-Deploy-Stage)

Description
The Azure DevOps pipeline is currently failing with the error:
"This pipeline needs permission to access a resource before this run can continue to TF-Deploy-Stage."

I have rechecked and confirmed that we already have the required permissions to approve the Terraform plan for Test, UAT, and Production environments in the EDI-Terraform project.

However, we do not have the required permission to grant/access the resource needed by the pipeline to proceed to the TF-Deploy-Stage.

Requesting to please provide the necessary access or approve the required pipeline resource so that the deployment can proceed without interruption.

If you want, I can tailor this further depending on whether it's a service connection / environment / variable group / secure file issue (that’s usually what this error points to).





Here’s a clean recreation of the service request based on the permission issue shown in image 3:

Short Description
Need permission to access required resource for Terraform deployment pipeline in Azure DevOps (TF-Deploy-Stage)

Description
The Azure DevOps pipeline is currently failing with the error:
"This pipeline needs permission to access a resource before this run can continue to TF-Deploy-Stage."

Requesting access to the required resource(s) used in the Terraform deployment pipeline for Test, UAT, and Production environments in the EDI-Terraform project.

This permission is required to proceed with the deployment stage (TF-Deploy-Stage) without manual intervention. Kindly grant the necessary access or approve the pipeline resource so deployments can continue successfully.

If you want, I can also make this more specific (e.g., service connection, variable group, environment approval) based on what resource the “View” button is pointing to.

